# GitHub Actions secrets for the LIVE F-net inference worker.
#
# FILL THIS IN (copy the two values from your locked worker/.env — do NOT let
# Claude read either file), then upload with ONE of the two methods in
# worker/deploy/SETUP_GHA.md and DELETE your filled-in copy.
#
# These are the ONLY secrets the live pipeline needs:
#   - waveform downloads authenticate to NIED with these credentials
#   - the data-branch push uses the automatic GITHUB_TOKEN (no setup)
#   - release assets (DB/checkpoint/config) are public -> no PAT needed

FNET_USERNAME=
FNET_PASSWORD=
