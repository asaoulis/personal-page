# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables (only .env.example is committed)
.env
.env.*
!.env.example

# vercel
.vercel/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# python (worker)
__pycache__/
*.pyc
.pytest_cache/
.vercel
.env*

# HinetPy / F-net download intermediates (raw win32 chunks + channel tables).
# The fetch_fnet.py adapter routes these to a /data scratch dir, but a manual run
# launched from the repo root would otherwise leave them here. Never commit them.
*.cnt
*.ch
*.euc.ch
public/live-data/
